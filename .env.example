# NVIDIA ASR to OpenAI API 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# 模型存储路径 (容器内路径)
MODEL_PATH=/data/model

# 模型闲置超时时间 (秒), 超时后自动卸载释放显存
MODEL_TIMEOUT_SEC=300

# 启用的模型列表 (逗号分隔)
# 可选值: canary-1b-v2, parakeet-tdt-0.6b-v3
ENABLED_MODELS=canary-1b-v2,parakeet-tdt-0.6b-v3

# 是否使用 FP16 半精度推理 (降低显存占用)
USE_FP16=true

# API 服务端口
API_PORT=8909

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# API Key 验证 (可选)
# 如果设置此变量, 则所有 API 请求都需要在 Header 中提供有效的 API Key:
# Authorization: Bearer <API_KEY>
# 如果不设置或留空, 则不进行 API Key 验证
# API_KEY=your-secret-api-key-here

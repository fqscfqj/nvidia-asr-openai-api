# requirements.txt
# NVIDIA Canary-1B-v2 语音识别 API 服务依赖

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# NVIDIA NeMo ASR 工具包 (包含 Canary 模型支持)
nemo_toolkit[asr]>=2.0.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0

# 深度学习框架 (已在基础镜像中, 但显式声明版本)
torch>=2.1.0
torchaudio>=2.1.0

# 模型下载与缓存
huggingface_hub>=0.19.0

# 异步支持
aiofiles>=23.0.0

# 日志与监控
loguru>=0.7.0

# 类型检查
pydantic>=2.0.0

# HTTP 客户端 (用于健康检查等)
httpx>=0.25.0
